import 'package:flutter/material.dart';

class AppStyles {
  static const panelHeader = TextStyle(
    fontSize: 18,
    fontWeight: FontWeight.bold,
    color: Colors.teal,
  );

  static const panelBody = TextStyle(
    fontSize: 16,
    color: Colors.black87,
  );
}
